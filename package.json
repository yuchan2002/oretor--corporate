{
  "name": "apex-retreat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx -y serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "engines": { "node": ">=18" }
}
